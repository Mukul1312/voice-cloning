# Python env (recreated on each machine — never commit)
.venv/
__pycache__/
*.pyc

# Firecrawl scrape dumps (research scratch)
.firecrawl/

# Model/weight caches (large; re-downloaded per machine)
*.pt
models/
pretrained_models/

# OS / editor cruft
.DS_Store

# Secrets — NEVER commit (the repo is public)
.hf_token
*.token

# Local skill cache
.claude/skills/
skills-lock.json
